apiVersion: v1
kind: Service
metadata:
  name: {{ include "multi-app.fullname" . }}
  labels:
    {{- include "multi-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "multi-app.selectorLabels" . | nindent 4 }}
