apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "multi-app.fullname" . }}-test-connection"
  labels:
    {{- include "multi-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "multi-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
