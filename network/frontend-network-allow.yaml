apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-external-to-frontend
  namespace: frontend-ns
spec:
  podSelector:
    matchLabels:
      app: frontend
  ingress:
  - from: []
    ports:
    - protocol: TCP
      port: 3000
  policyTypes:
  - Ingress