apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  namespace: monitoring
spec:
  selector:
    app: prometheus
  ports:
  - port: 9090
    targetPort: 9090
  type: NodePort

# kubectl expose deployment prometheus \
#  --name=prometheus-service \
#  --port=9090 \
#  --target-port=9090 \

