apiVersion: v1
kind: Service
metadata:
  name: bookstore-frontend-svc
spec:
  type: NodePort
  selector:
    app: bookstore-frontend
  ports:
    - port: 80          # service port
      targetPort: 80    # container port
      nodePort: 30005   # external port on each node (must be in allowed range)
