apiVersion: v1
kind: Service
metadata:
  name: bookstore-backend-svc
  labels:
    app: bookstore-backend
spec:
  type: ClusterIP
  selector:
    app: bookstore-backend
  ports:
    - name: http
      port: 8080       # Service port used by clients (frontend)
      targetPort: 5555 # Container port above
